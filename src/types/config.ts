export namespace Config {
  export const PROJECTED_SPLAT_STRIDE = 64;
  export const DEFAULT_MAX_SPLATS_PER_TILE = 512;
  export const MAX_TILES_PER_SPLAT = 16;
  export const WORKGROUP_SIZE = 256;
}